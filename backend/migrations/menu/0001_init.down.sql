DROP TABLE IF EXISTS notification_channels, landing_blocks, service_requests,
    order_line_options, order_lines, orders, options, option_groups,
    menu_item_allergens, menu_items, categories, tables, restaurants CASCADE;
