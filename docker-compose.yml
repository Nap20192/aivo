services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: aivo_menu
      POSTGRES_USER: aivo_menu
      POSTGRES_PASSWORD: aivo_menu
    ports:
      - "5432:5432"
    volumes:
      - aivo_menu_pgdata:/var/lib/postgresql/data

volumes:
  aivo_menu_pgdata:
